{
  "name": "vacatures-api",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply vacatures-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply vacatures-db --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260601.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
